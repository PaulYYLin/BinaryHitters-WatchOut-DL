# Fall Detection System - Environment Configuration
# This file contains environment-specific settings (paths, API endpoints, secrets)
# For algorithm parameters, see config.yaml
#
# Copy this file to .env and update with your values


# ==================== API Settings ====================
# Backend API base URL
API_BASE_URL=https://your-api.com

# API endpoint for fall detection video upload (POST /upload)
# Uploads video file with device_uid
API_UPLOAD_ENDPOINT=/upload

# API endpoint for device check-in (GET /device/checkin/{device_uid})
# The system will automatically append the device_uid to this endpoint
API_CHECKIN_ENDPOINT=/device/checkin

# Device unique identifier for this fall detection device
# This will be sent with video uploads and check-in requests
DEVICE_UID=your-device-uid-here

# Optional API key for authentication
# Leave empty if not needed
API_KEY=

# API request timeout (seconds)
API_TIMEOUT=30

# Number of retry attempts for API calls
API_RETRY_ATTEMPTS=3

# Device check-in interval (seconds)
# How often to send check-in requests to the server
# Default: 60 seconds (1 minute)
CHECKIN_INTERVAL=60


# ==================== File Paths ====================
# Directory for temporary video files
TEMP_DIR=/tmp/fall_events

# Directory for log files
LOG_DIR=./logs

# Path to MediaPipe pose model
MODEL_PATH=./src/utils/pose_landmarker_lite.task

# Path to configuration file
CONFIG_PATH=./config.yaml
